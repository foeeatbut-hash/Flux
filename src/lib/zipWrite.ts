/**
 * Прежнее место сборщика zip.
 *
 * Сам сборщик переехал в общую папку: архив теперь собирают двое — окно для
 * выгрузки .docx и сервер для пакета диагностики. Реэкспорт оставлен, чтобы
 * выгрузка Word не заметила переезда: она проверяется отдельным набором
 * (`scripts/test-docx.ts`), и трогать её ради чужой задачи незачем.
 */
export { crc32, zip, type Deflate, type ZipEntry } from '../../feedback/zip';
